<%= form_with(model: hackrom, local: true) do |form| %>
  <% if hackrom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hackrom.errors.count, "error") %> prohibited this hackrom from being saved:</h2>

      <ul>
      <% hackrom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nomeRom %>
    <%= form.text_field :nomeRom, id: :hackrom_nomeRom %>
  </div>

  <div class="field">
    <%= form.label :jogo_id %>
    <%= form.text_field :jogo_id, id: :hackrom_jogo_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
